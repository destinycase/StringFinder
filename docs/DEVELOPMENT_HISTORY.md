# StringFinder 버전별 개발 히스토리

이 문서는 날짜가 아닌 버전 단위로 Git 커밋과 당시 문서를 추적해 정리한 기록입니다. 버전별 내용은 커밋 메시지와 실제 변경 파일을 대조해 작성했으며, 기능을 명시한 근거가 없는 초기 버전은 확인 가능한 범위만 기록했습니다.

## 초기 버전

### v1.0.0

- 첫 번째 명시적 제품 버전입니다.
- 이후 2.x 개발의 기준이 된 초기 검색 애플리케이션 상태를 확립했습니다.

### v2.4.0, v2.4.4

- 2.x 버전 계열의 초기 릴리스와 유지보수 버전입니다.
- 당시 커밋은 버전 갱신 중심이며, 현재 구조와 직접 연결되는 세부 기능 계약은 남아 있지 않습니다.

### v2.6.0, v2.7.0, v2.8.0, v2.9.0, v2.9.1

- 초기 GUI·검색 기능을 빠르게 확장한 연속 릴리스입니다.
- 중간에 `src`, `tests`, `scripts` 디렉터리를 삭제·재구성한 커밋이 있어, 버전 간 파일 구조가 연속적이지 않습니다.

### v3.0.0, v3.0.1

- 3.x 제품 계열로 전환했습니다.
- 버전 갱신과 배포 스냅샷이 중심이며, 후속 버전에서 구조가 다시 교체되었습니다.

### v3.5.1, v3.6.0, v3.6.1, v3.7.4

- 3.x 유지보수 및 배포 버전입니다.
- 이 구간에도 이전 산출물을 정리하고 다음 구조를 추가하는 커밋이 포함되어 있어, 세부 동작은 해당 커밋의 스냅샷으로 확인해야 합니다.

## v4 계열

### v4.19.1, v4.23.0, v4.23.1

- 4.x 개발 계열의 기반 릴리스와 소규모 후속 버전입니다.
- 배포 가능한 애플리케이션 구조를 유지하면서 버전별 파일 스냅샷을 갱신했습니다.

### v4.34.0, v4.40.1, v4.43.0, v4.44.0

- 기능 확장과 배포 구조 정리를 반복한 릴리스입니다.
- 일부 버전 사이에는 테스트·스크립트·소스 디렉터리 재구성이 있어, 현재의 모듈 구조로 직접 이어지는 단일 리팩터링으로 해석하지 않습니다.

### v4.48.0, v4.49.0, v4.50.0, v4.51.0, v4.53.0, v4.63.8

- 4.x 후반 배포 버전입니다.
- 안정화와 버전 갱신이 중심이며, v5 계열로 넘어가기 전의 제품 스냅샷을 형성했습니다.

## v5 초기 계열

### v5.0.1, v5.1.0, v5.1.1

- 현재 계열의 버전 체계를 시작한 릴리스입니다.
- 이후 Rust 엔진 도입 전까지 업로드·배포 중심 커밋이 많아, 특정 기능의 도입 시점은 커밋 diff로 확인해야 합니다.

### v5.4.0, v5.4.2

- 현재 저장소에서 추적 가능한 안정화 계열의 시작점입니다.
- 이후 Rust 기반 검색 엔진과 테스트 체계가 도입되기 전의 Python 중심 제품 상태입니다.

### v5.5.1

- v5.4 계열의 후속 기능·배포 버전입니다.

### v5.6.1, v5.6.2, v5.6.3, v5.6.4

- 문맥 미리보기와 애플리케이션 안정성을 단계적으로 보완했습니다.
- v5.6.3은 안정성 개선 및 버전 갱신, v5.6.4는 문맥 미리보기 표시 개선을 명시합니다.

### v5.6.5, v5.6.6, v5.6.7

- 반복 배포와 누락 파일 안내 보완을 진행했습니다.
- v5.6.7에는 배포 버전 갱신과 누락 파일 업데이트가 함께 기록되어 있습니다.

### v5.7.1, v5.7.2

- 마이너 릴리스와 개발 문서 업데이트를 진행했습니다.
- 이후 5.8 계열에서 현지화·구조 검색·리소스 보호 작업이 본격화됩니다.

## v5.8 계열: 구조 검색과 보호 기능

### v5.8.1

- 5.8 계열을 시작한 배포 버전입니다.

### v5.8.2

- UI 현지화 대응을 확대했습니다.
- 검색 결과와 건너뛴 사유가 언어 설정에 따라 표시되도록 기반을 마련했습니다.

### v5.8.3

- JSON 스트리밍 처리와 벤치마크 영구 기록을 도입·정리했습니다.
- Rust/Python 구조 검색의 결과와 제한을 비교할 수 있는 성능 기록 기반을 마련했습니다.

### v5.8.4

- 검색 제한 안내와 장치별 리소스 가드를 개선했습니다.
- 시스템 전체 메모리 비율만이 아니라 장치 메모리와 프로세스 한도를 고려하는 방향으로 전환했습니다.

### v5.8.5

- 설정·진단 및 배포 버전을 갱신했습니다.

### v5.8.6

- Excel 미리보기와 결과 표시를 보완하고 배포본을 갱신했습니다.

### v5.8.7

- 고급 설정 그룹과 검색 옵션 구성을 정리했습니다.

### v5.8.8

- 휴지통 파일 제외와 검색 오류·타입 오류 방어를 반영했습니다.

### v5.8.9

- 5.8 계열 안정화 배포를 마무리했습니다.

## v5.9 계열: 검색 계약·진단·UX 안정화

### v5.9.1

- 성능 및 안정성을 개선하고 벤치마크 기준을 정리했습니다.

### v5.9.2

- 단일 파일 검색 종료 시 감시 스레드를 GIL 해제 구간에서 종료하도록 수정했습니다.
- 검색 완료 후 감시 스레드와의 교착 가능성을 제거했습니다.

### v5.9.3

- 검색 무결성과 성능을 보완했습니다.
- Rust 코드 포맷 표준화, QA 후속 정리, XLSX 메모리 최적화 검토와 벤치마크 문서를 반영했습니다.

### v5.9.4

- FHD 화면에서 더 많은 결과를 볼 수 있도록 목록 밀도와 화면 공간을 개선했습니다.
- 배포본을 재생성했습니다.

### v5.9.5

- 검색 UI와 배포 안정성을 개선했습니다.
- 검색 방식 선택, 설정 콤보박스, 결과 요약 표시를 정리했습니다.

### v5.9.6

- QA에서 발견된 현지화 누락과 예외 처리 문제를 수정했습니다.

### v5.9.7

- 검색 사유 모듈과 계약 문서를 분리했습니다.
- 대형 모듈의 전면 분리는 파편화 위험 때문에 채택하지 않고, 유지보수에 필요한 경계만 정리했습니다.

### v5.9.8

- `정밀 검색` 명칭을 `누락 방지 검색(매우 느림)`으로 통일했습니다.
- 한국어 변경에 맞춰 영어 UI와 안내 문구를 대응했습니다.

### v5.9.9

- 설정값과 설정 초기값을 버전으로 관리했습니다.
- 다음 실행 시 설정 버전이 현재 기본값과 다르면 해당 설정을 안전하게 갱신하도록 했습니다.

### v5.9.10

- 배포본에서 실행할 수 있는 성능 진단을 설정 화면으로 이동했습니다.
- 진단 진행·취소·반복 측정과 별도 결과 창을 추가했습니다.

### v5.9.11

- 성능 진단 리포트의 정확성·사용성을 개선했습니다.
- 여러 회차의 개별 판정과 사람이 읽을 수 있는 요약을 기록하도록 했습니다.

### v5.9.12

- 로그를 별도 창으로 분리하고 검색 결과 영역을 확장했습니다.
- 검색 결과 탭과 상단 여백을 줄여 결과 표시 공간을 확보했습니다.

### v5.9.13

- 구조화 결과 내보내기와 검색 오류 안내를 개선했습니다.
- 세션 저장 실패 보호, 내보내기 원자적 교체, 설정 타입 검증, Excel 동시 처리 적용 범위를 보완했습니다.

### v5.9.14

- 검색 결과 표의 셀 좌우 여백을 조정해 가독성을 개선했습니다.

### v5.9.15

- UI 정렬과 탭 메뉴를 개선했습니다.
- 탭 추가 동작을 컨텍스트 메뉴로 이동하고 Excel 동시 처리 설정을 정리했습니다.

### v5.9.16

- 성능 진단 UI의 현지화 누락을 보완했습니다.
- 세션 안내문, 창 상태 타입 검증, Excel 임시 파일 정리와 저장 실패 보호를 강화했습니다.

### v5.9.17

- 현지화 과정에서 남은 중복 콜백과 진단 레거시 구현을 정리했습니다.
- 검색·세션·진단 관련 회귀 검증을 유지했습니다.

### v5.9.18

- 대형 단일 라인 문서의 매치 주변 문맥을 제한했습니다.
- 누락 방지 검색이 여러 매치 위치를 보존하고, 미리보기가 첫 매치만 기준으로 표시되지 않도록 개선했습니다.
- Rust 엔진·Python 경로·UI 모델과 관련 회귀 테스트를 함께 갱신했습니다.
- README와 사용자·개발자 가이드의 버전 표기를 5.9.18로 맞췄습니다.

## 현재 설계로 이어진 결정

- 일반 검색은 Rust 엔진의 속도를 우선하고, 누락 방지 검색은 유니코드와 손상 인코딩 대응을 우선합니다.
- 구조화 문서는 검색어가 먼저 발견되어도 구문 검증을 완료한 뒤 결과를 확정합니다.
- 제한·오류·접근 실패는 정상 매치와 분리된 스킵 사유로 전달합니다.
- 사용자 표시 문자열은 한국어와 English 리소스에 함께 등록합니다.
- 상세 성능 수치는 `benchmark_history.md`와 `ENGINE_PERFORMANCE_BASELINE.md`에서 관리합니다.

## 이력 검증 방법

특정 버전의 실제 변경을 재확인하려면 다음 명령을 사용합니다.

```powershell
git show --stat <커밋>
git show <커밋> -- <파일>
git diff <이전 커밋>..<현재 커밋> -- <파일>
```

초기 버전처럼 커밋 메시지가 단순한 경우에는 해당 커밋의 파일 스냅샷과 다음 버전의 차이를 함께 확인해야 하며, 이 문서의 요약을 세부 API 계약으로 사용해서는 안 됩니다.
